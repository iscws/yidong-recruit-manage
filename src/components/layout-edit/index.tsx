import React from 'react';
import type { FC, ReactNode } from 'react';

interface LayoutEditProps {
  children?: ReactNode;
}

const LayoutEdit: FC<LayoutEditProps> = () => {
  return <div>LayoutEdit</div>;
};

export default LayoutEdit;
